instances:
   time:
      model: tame/time                  # model type [default=time]
      initialization:
         concentration: 20.0            # Concentration [mM; min=0.0; default=0.0]
      coupling:
         previous_time: current_time    # Previous time [s; default=]
         previous: current              # Previous concentration [mM; default=]
